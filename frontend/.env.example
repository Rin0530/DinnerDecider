# Next.js Environment Variables
# API Base URL for client-side requests (must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# Backend URL for server-side API proxy (used in next.config.js)
BACKEND_URL=http://localhost:8080
